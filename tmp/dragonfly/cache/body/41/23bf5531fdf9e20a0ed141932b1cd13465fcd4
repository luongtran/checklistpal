(function() {
  jQuery(function() {
    return $('.checkbox').sortable({
      axis: 'y',
      update: function() {
        return $.post($(this).data('update-url'), $(this).sortable('serialize'));
      }
    });
  });

}).call(this);
