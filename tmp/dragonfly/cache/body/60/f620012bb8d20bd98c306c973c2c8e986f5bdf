(function() {
  jQuery(function() {
    return $('#list-items').sortable({
      axis: 'y',
      update: function() {
        return $.post($(this).data('update-url'), $(this).sortable('serialize'));
      }
    });
  });

}).call(this);
